#include <stdio.h>

int main(){
    float base =
    printf("Digite a base do retangulo: ", base);
    scanf("%f", &base);
    float h =
    printf("\nDigite a altura do retangulo: ", h);
    scanf("%f", &h);
    float area = base*h;
    printf("\nA area do seu retangulo e: %f", area);

    return 0;
}
